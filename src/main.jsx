import React from "react";
import ReactDOM from "react-dom/client";
import BugDashboard from "./BugDashboard";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <BugDashboard />
  </React.StrictMode>
);
